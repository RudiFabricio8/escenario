-- migrate.sql
-- Este archivo está reservado para futuras migraciones y cambios en el esquema
-- una vez que la base de datos ya está en producción (ej. ALTER TABLE, DROP COLUMN).

-- Como es el despliegue inicial, el esquema completo ya fue creado en 01_schema.sql.
-- Se incluye este archivo para cumplir con la estructura requerida.

SELECT 'Migraciones completadas' as status;
