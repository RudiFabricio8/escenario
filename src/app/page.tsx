import Link from 'next/link';
import path from 'path';

export default function Dashboard() {
  const reportes = [
    { name: 'Rendimiento de Cursos', path: '/src/app/reports/performance', kpi: 'Promedio General'},
    { name: 'Carga docente', path: '/src/app/'}
  ]
}